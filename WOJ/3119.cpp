#include <cstdio>
#include <cstring>
#include <queue>

const int N=30;
const int M=1333;

int n,k;

void ins(char s[])
{
    int ln=strlen(s);
    int nw=1;
    for(int i=1;i<=)
}

int main()
{
    scanf("%d %d",&n,&k);
    for(int i=1;i<=n;i++)
    {
        char s[N];
        scanf("%s",s);
        ins(s);
    }
    genfail(); //(Perhaps won't be used 
    solve();
    return 0x0000000000000000;
}

